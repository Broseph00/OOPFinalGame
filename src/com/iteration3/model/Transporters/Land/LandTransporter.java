package com.iteration3.model.Transporters.Land;

import com.iteration3.model.Transporters.Transporter;

public abstract class LandTransporter extends Transporter {
    public LandTransporter(int maxMove, int cap) {
        super(maxMove, cap);
    }
}
