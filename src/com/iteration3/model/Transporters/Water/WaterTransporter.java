package com.iteration3.model.Transporters.Water;

import com.iteration3.model.Transporters.Transporter;

public class WaterTransporter extends Transporter {
    public WaterTransporter(int maxMove, int cap) {
        super(maxMove, cap);
    }
}
